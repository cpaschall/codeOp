<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta"></div>
        {{user.name}}
        <div class="text">
            {{commentData}}
        </div>

    </section>
    {{/each}}
</div>